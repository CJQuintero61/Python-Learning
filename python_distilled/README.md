# Python Distilled

This directory holds the chapter notes I took as I followed along in the book
Python Distilled by David M. Beazley.

## Setting Up A Venv (Windows)

1. `python -m venv .venv`
2. `.venv\Scripts\activate`
3. `pip install -r requirements.txt`
